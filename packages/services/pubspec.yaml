name: services
description: Service boundaries for payments and backend orchestration.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.10.4"

dependencies:
  flutter:
    sdk: flutter
  data:
    path: ../data
  domain:
    path: ../domain
  supabase_flutter: ^2.8.0
