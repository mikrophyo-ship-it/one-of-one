name: customer_app
description: One of One customer marketplace app.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: "^3.10.4"

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.4.1
  file_picker: ^8.1.7
  flutter_dotenv: ^5.2.1
  mobile_scanner: ^5.2.3
  supabase_flutter: ^2.8.0
  core_ui:
    path: ../../packages/core_ui
  data:
    path: ../../packages/data
  domain:
    path: ../../packages/domain
  services:
    path: ../../packages/services
  utils:
    path: ../../packages/utils
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
